pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code cloned from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage executed'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage executed'
            }
        }
    }
}
